# Basic Android App

This repository contains a minimal Android application written in Kotlin. It includes a single `MainActivity` that displays a greeting.

## Building

Open the project with Android Studio or build with Gradle:

```bash
./gradlew assembleDebug
```

A Gradle wrapper is not included, so ensure you have a compatible version of Gradle installed.
